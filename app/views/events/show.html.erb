<!--
<%- model_class = Event -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:location) %>:</strong></dt>
  <dd><%= @event.location %></dd>
  <dt><strong><%= model_class.human_attribute_name(:name) %>:</strong></dt>
  <dd><%= @event.name %></dd>
  <dt><strong><%= model_class.human_attribute_name(:description) %>:</strong></dt>
  <dd><%= @event.description %></dd>
  <dt><strong><%= model_class.human_attribute_name(:url) %>:</strong></dt>
  <dd><%= @event.url %></dd>
  <dt><strong><%= model_class.human_attribute_name(:event_handler_id) %>:</strong></dt>
  <dd><%= @event.event_handler_id %></dd>
  <dt><strong><%= model_class.human_attribute_name(:starts_at) %>:</strong></dt>
  <dd><%= l(@event.starts_at, format: '%d %B %Y, %H:%M') %></dd>
</dl>
-->

<div class="block">
  <div class="block-title">
    <h2>
      <strong><%= @event.name %></strong>
    </h2>
  </div>

  <div class="row">
    <div class="col-md-6">
      image
      </p>
    </div>

    <div class="col-md-6">
      <table class="table">
        <tr>
          <td><strong>Время</strong></td>
          <td><%= l(@event.starts_at, format: '%d %B %Y, %H:%M') %></td>
        </tr>
        <tr>
          <td><strong>Место</strong></td>
          <td><%= @event.location %></td>
        </tr>
        <tr>
          <td><strong>Ссылка</strong></td>
          <td><%= link_to @event.url, @event.url %></td>
        </tr>
        <tr>
          <td><strong>Организатор</strong></td>
          <td><%= link_to @event.event_handler.name,
                          event_handler_path(@event.event_handler)
          %></td>
        </tr>
      </table>
    </div>
  </div>
</div>

<div class="block">
  <div class="block-title">
    <h3>Описание</h3>
  </div>

  <p><%= simple_format(event_description) %></p>
</div>
